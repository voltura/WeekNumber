Application icon improvement (if working with NotificationIcon)
===============================================================
potentially, to get correct looking week number icon (not jagged/squewed), create icon using class IconFactory instead of 
single resolution icon as per today, this would then potentially make windows select correct resolution to display from within
the icon images in different resolutions